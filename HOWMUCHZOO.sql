CREATE TABLE MONEY(
	HANDMONEY NUMBER
);

SELECT HANDMONEY FROM MONEY;

CREATE TABLE COW(
	SELL_DATE DATE DEFAULT SYSDATE,
	POINT NUMBER NOT NULL,
	PRICE NUMBER NOT NULL
);

SELECT * FROM COW;

CREATE TABLE PIG(
	SELL_DATE DATE DEFAULT SYSDATE,
	POINT NUMBER NOT NULL,
	PRICE NUMBER NOT NULL
);

SELECT * FROM PIG;

CREATE TABLE DOG(
	SELL_DATE DATE DEFAULT SYSDATE,
	POINT NUMBER NOT NULL,
	PRICE NUMBER NOT NULL
);

SELECT * FROM DOG;

CREATE TABLE CAT(
	SELL_DATE DATE DEFAULT SYSDATE,
	POINT NUMBER NOT NULL,
	PRICE NUMBER NOT NULL
);

SELECT * FROM CAT;

INSERT INTO MONEY
VALUES (10000);

SELECT HANDMONEY FROM MONEY;

COMMIT;














